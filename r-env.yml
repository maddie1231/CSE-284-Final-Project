# environment.yml
name: r_marker_env
channels:
  - conda-forge
  - defaults
dependencies:
  - r-base=4.2
  - r-ggplot2
  - r-data.table
  - r-qqman
  - r-devtools  # optional, useful for installing other R packages
